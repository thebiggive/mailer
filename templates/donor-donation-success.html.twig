{% extends renderHtml ? '_base.html.twig' : '_base.text.twig' %}

{% block content %}
  <p>Dear {{ donorFirstName }},</p>

  <p>This email is the official receipt for your donation.</p>

  <p>Thank you for supporting {{ charityName }} with your generous gift through the Big Give.<p>

  {% if campaignThankYouMessage %}
    <p>A message from {{ charityName }}:</p>
    <blockquote>{{ campaignThankYouMessage | nl2br }}</blockquote>
  {% endif %}

  <p>Here is your donation information:</p>

  <ul>
    <li>Total for {{ charityName }}: <strong>{{ totalCharityValueAmount | format_currency('GBP') }}</strong></li>
    <li>Donation: <strong>{{ donationAmount | format_currency('GBP') }}</strong></li>
    <li>Gift Aid: <strong>{{ giftAidAmountClaimed | format_currency('GBP') }}</strong></li>
    {% if matchedAmount > 0 %}
        <li>Match Funds: <strong>{{ matchedAmount | format_currency('GBP') }}</strong></li>
    {% endif %}
  </ul>

  <p>
    Donation reference: <strong>{{ transactionId }}</strong><br>
    Reference on credit/debit card statement: {{ statementReference | default("Enthuse.com") }}
  </p>

  {% if tipAmount > 0 %}
    <p>Thank you for leaving a tip of <strong>{{ tipAmount | format_currency('GBP') }}</strong> toward the Big Give. We are a charity too, and your donation helps us to keep developing the platform and to run match funding campaigns. Thank you for your support.</p>
  {% endif %}

  <p>Total amount you have been charged is <strong>{{ totalChargedAmount | format_currency('GBP') }}</strong>.</p>

  <p>If you have any questions, please contact us.</p>
{% endblock %}
