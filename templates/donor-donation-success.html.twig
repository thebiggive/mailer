{% extends renderHtml ? '_base.html.twig' : '_base.text.twig' %}

{% block content %}
  <p>Dear {{ donorFirstName }} {{ donorLastName }},</p>

  <p>Thank you for supporting {{ charityName }} with your generous gift through the Big Give.<p>

  <p>Here is your donation information:</p>

  <ul>
    <li>Total for {{ charityName }}: <strong>{{ totalCharityValueAmount | format_currency('GBP', {min_fraction_digit: 0, max_fraction_digit: 2}) }}</strong></li>
    <li>Donation: <strong>{{ donationAmount | format_currency('GBP', {min_fraction_digit: 0, max_fraction_digit: 2}) }}</strong></li>
    <li>Gift Aid: <strong>{{ giftAidAmountClaimed | format_currency('GBP', {min_fraction_digit: 0, max_fraction_digit: 2}) }}</strong></li>
    {% if matchedAmount > 0 %}
        <li>Match Funds: <strong>{{ matchedAmount | format_currency('GBP', {min_fraction_digit: 0, max_fraction_digit: 2}) }}</strong></li>
    {% endif %}
  </ul>

  <p>
    Donation reference: <strong>{{ transactionId }}</strong><br>
    Reference on credit/debit card statement: CharityCheckout.co.uk
  </p>

  {% if tipAmount > 0 %}
    <p>Thank you for leaving a tip of <strong>{{ tipAmount | format_currency('GBP', {min_fraction_digit: 0, max_fraction_digit: 2}) }}</strong> toward the Big Give. We are a charity too, and your donation helps us to keep developing the platform and to run match funding campaigns. Thank you for your support.</p>
  {% endif %}

  <p>Total amount you have been charged is <strong>{{ totalChargedAmount | format_currency('GBP', {min_fraction_digit: 0, max_fraction_digit: 2}) }}</strong>.</p>

  {% if campaignThankYouMessage %}
    <p>A message from {{ charityName }}:</p>
    <blockquote>{{ campaignThankYouMessage | nl2br }}</blockquote>
  {% endif %}

  <p>If you have any questions, please contact us.</p>
{% endblock %}
