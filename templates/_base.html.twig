<!DOCTYPE html>
<html lang="en-GB">
  <head>
    <title>{{ subject }}</title>
    <style type="text/css">
      p, ul, li, blockquote {
        font-size: 0.9rem;
      }

      blockquote {
        font-style: italic;
      }

      .container {
        background: white;
        color: black;
        padding: 0.9rem;
        border-radius: 8px;
        margin: 0.9rem auto;
        max-width: 500px;
      }

      .content {
        text-align: left;
      }

      .headerImg {
        max-width: 50%;
        height: auto;
        margin: 1rem 0;
      }
    </style>
  </head>
<body>
  <div class="container">
    <img class="headerImg" src="{{ headerImageRef }}" alt="The Big Give"/>
    <div id="content" class="content">
      {% block content %}{% endblock %}
    </div>
    <div id="footer">
      {% block footer %}
        <p>
          Kind regards,<br>
          The Big Give Team<br>
          www.thebiggive.org.uk<br>
        </p>
      {% endblock %}
    </div>
  </div>
</body>
</html>
